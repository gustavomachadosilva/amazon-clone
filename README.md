# Mercatto

A marketplace project (Amazon-like) being built for a college course.

## Current state

Nothing implemented yet. `ui_generated_by_IA/` holds design material (an HTML prototype and a
design system/token set) — raw input for an upcoming pass to organize it into real UI files, not
yet a locked spec for the whole app.

Stack, structure, and scope are still open decisions.
